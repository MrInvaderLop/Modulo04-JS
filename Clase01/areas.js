// Ejercicio propuesto
const calculararea = (width, height) => {
    let base = width;
    const multiplier = 2;
    return base * height * multiplier;
};
console.log(calculararea(5,10));

// Simplificacion personal
const calcularArea = (width, height) => {
    return width * height;
};
console.log(calcularArea(5,10));